{
  "name": "pg-webiste",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:phone": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host",
    "tunnel": "npx --yes localtunnel --port 5173"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  }
}
